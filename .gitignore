# Local overrides (personal, not shared)
CLAUDE.local.md

# Tool databases (per-project, regenerated by setup.sh)
.memelord/
.codemogger/
.cachebro/
.me/

# Session and runtime data
*.local.*

# Secrets and credentials (never commit)
.env
.env.*
*.key
*.pem
*.p12
credentials.*
*secret*
